{
  "type": "claude_code_cli",
  "name": "Claude Code 2.1.88 Repo Reader",
  "nodePath": "node",
  "cliPath": "${CLAUDE_CODE_CLI_PATH}",
  "dangerouslySkipPermissions": true,
  "maxTurns": 16,
  "timeoutMs": 420000,
  "systemPrompt": "Prefer exact fact extraction from local files. Keep tool use minimal, read only the files named in the task, and write the requested JSON exactly.",
  "extraArgs": [
    "--allowedTools",
    "Read,Write,Edit,Grep,Glob,LS"
  ]
}
